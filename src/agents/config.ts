/**
 * Default Anthropic model used by all agents when the ANTHROPIC_MODEL
 * environment variable is not set. Keep this as the single source of truth
 * so a model bump only needs to change one line.
 */
export const DEFAULT_MODEL = 'claude-sonnet-4-6';
